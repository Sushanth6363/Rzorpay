# JUDGE GUIDE — Unified Recovery Engine

**Milestone**: M8 — Judge-Ready End-to-End Demonstration, Reproducibility & Validation  
**Date**: 2026-09-01  
**Project**: Razorpay Track 3 AI Revenue Recovery Buildathon  

---

## 1. Executive Summary & Prototype Boundary

The **Unified Recovery Engine** is an intelligent, multi-tenant AI decision and closed-loop recovery orchestration system designed to prevent revenue loss from failed payments while protecting customer experience.

### ⚠️ Critical Prototype Boundary Notice
- **This is a sandbox/simulator prototype**, not a live Razorpay production integration.
- **Zero External Credentials**: Does NOT call live Razorpay APIs, payment gateways, WhatsApp/SMS gateways, PostgreSQL, Redis, Kafka, or paid LLM APIs.
- **Deterministic Local Execution**: Pure Python 3.12.9 + SQLite WAL + CatBoost S-Learner running 100% locally.
- **Simulated Recovery**: Payments, outreach deliveries, gateway latencies, and outages are generated by a stateful deterministic sandbox simulator (`SandboxSimulator`).

---

## 2. Quick Start / Clean-Start Launcher

### Option A: One-Command Python Demo Launcher
```bash
python run_demo.py
```
*(Runs environment verification script and launches Streamlit on `http://localhost:8501`)*

### Option B: Direct Streamlit Launch
```bash
streamlit run ui_app.py
```

### Option C: Run Full Regression Test Matrix (163/163 Tests)
```bash
pytest -v
```

---

## 3. Recommended Judge Exploration Flow

When the Streamlit Dashboard launches, explore the 5 dedicated tabs:

### Step 1: Scenario Explorer (`Tab 1`)
1. Select any of the **12 Pre-Configured Golden Demo Scenarios** from the dropdown.
2. Observe the full correlation trace from event ingestion to Stage 0/1, candidate generation, safety filters, AI decision, contact ledger, sandbox execution, reconciliation, and attribution.
3. Key scenarios to test:
   - **Scenario 1 (Successful Retry)**: Intervention delivered and payment recovered (attributed > ₹0).
   - **Scenario 3 (Natural Self-Cure)**: $NO\_ACTION$ baseline selected; payment occurs naturally. Observe **₹0 AI Attribution**.
   - **Scenario 4 (Self-Cure Before Delivery)**: Payment occurs before outreach delivery. Attribution engine forces **₹0 AI Attribution** (`INV-8`).
   - **Scenario 5 (Gateway Outage)**: Active outage on HDFC. Observe hard safety filter rejecting retries regardless of EV (`INV-4`).
   - **Scenario 6 (Contact Budget Exhausted)**: Customer reached max contact cap (5). Atomic reservation fails (`INV-2`).
   - **Scenario 8–10 (Execution Unknown)**: Ambiguous delivery status triggering reconciliation ladder (`RECONCILED_DELIVERED`, `RECONCILED_NOT_SENT`, or conservative slot hold `RECONCILED_UNRESOLVED`).

### Step 2: 5-Arm Experiment Benchmark (`Tab 2`)
1. Set seed range (e.g. `21` to `25`) and click **🚀 Run 5-Arm Benchmark**.
2. Inspect the **Primary Pre-Registered Metric (`A2 vs A1`)**: Incremental recovery rate, 95% Newcombe confidence interval, p-value, and statistical verdict (`STATISTICALLY_SIGNIFICANT` vs `INCONCLUSIVE`).
3. Inspect the **5-Arm Performance Summary Table**: `CONTROL`, `A1`, `A2ns`, `A2`, `A3`, `A5`.
4. Inspect the **Secondary Comparisons Table**: Multiplicity controlled via Holm-Bonferroni step-down correction.

### Step 3: Safety Invariants & Contact Budget (`Tab 3`)
1. View the live checklist of invariants `INV-1` through `INV-9`.
2. Use the **Interactive Outage Simulator** to toggle outages on HDFC/ICICI/RAZORPAY and observe immediate safety suppression.

### Step 4: Retraining Tuple Inspector (`Tab 4`)
1. Inspect `TrainingRecord` tuples ($X, A, Y$).
2. Observe `PointInTimeLeakageError` enforcement preventing post-decision fields (`gross_recovered_paise`, `self_cured`, `payment_outcome`) from entering feature vector $X$ (`INV-7`).

---

## 4. Key AI Concepts Explained

### 1. Incremental Recovery Uplift (Not Direct Recovery)
The AI does **not** "recover money" directly. It estimates:
$$\Delta P = P(Y=1 \mid X, \text{action}) - P(Y=1 \mid X, \text{NO\_ACTION})$$
An intervention is recommended **only if** $\Delta P > 0$ and Expected Value ($\Delta P \times \text{Amount} - \text{Cost}$) is positive.

### 2. Attribution Rule: Intervention Recovery vs Self-Cure (`INV-8`)
- **Intervention Recovered**: Delivered intervention followed by payment within attribution window $\rightarrow$ 100% of recovery value attributed to AI.
- **Self-Cure**: Payment under `NO_ACTION`, uncontacted baseline, or payment before outreach delivery $\rightarrow$ **₹0 AI Attribution**.

### 3. Contact Cap & Atomic Reservation (`INV-2`)
Contacts are capped (e.g. 5 attempts per customer/month). Budget reservation executes via atomic conditional SQL (`UPDATE contact_budgets SET reserved = reserved + 1 WHERE reserved + consumed < cap`). Failed reservations safely fall back to uncontacted evaluation without slot leakage.

---

## 5. Summary of Architecture Invariants (`INV-1` to `INV-9`)

- `INV-1` Tenant Isolation: Merchant ID salting for arm hashing and budget isolation.
- `INV-2` Atomic Contact Reservation: Database-enforced conditional reservation.
- `INV-3` Exploration Safety: Safety-rejected candidates cannot be chosen by exploration.
- `INV-4` Gateway Outage Safety: Known gateway outages reject retry actions.
- `INV-5` Retry Ownership: Single active recovery owner per opportunity.
- `INV-6` Execution Unknown: Ambiguous delivery resolves via reconciliation ladder.
- `INV-7` Point-in-Time Features: Post-decision leakage blocked from training vector.
- `INV-8` Self-Cure Attribution: Self-cures get strictly ₹0 AI attribution.
- `INV-9` NO_ACTION Counterfactual: Uncontacted baseline evaluated first.
